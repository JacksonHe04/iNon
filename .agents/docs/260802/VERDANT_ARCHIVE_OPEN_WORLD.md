# iNon「绿迹世界」开放世界阶段设计与实现记录

日期：2026-08-02
状态：持续迭代中（本文记录当前可运行基线，不代表最终品质上限）

> 最新纠偏：用户明确否决“多个资料场所散落在远方”的地图结构。主世界已撤下九处内容地标，空间只保留一座可直接步入的中心住宅；收藏与高密度数据将集中进入这座住宅，户外只允许出现符合地理与用途逻辑的自然地貌、桥梁、测绘/生存设施和生态。本文后半部保留的“九个小世界”内容仅作为已废弃方案的历史记录，不再代表当前目标。

关联文档：

- [完整 10 轮设计问答与迁移规格](./RETRO_GREEN_UI_REDESIGN_SPEC.md)
- [原 UI 风格分析](./UI_DESIGN_STYLE_ANALYSIS.md)
- [视觉纠偏记录](./VERDANT_ARCHIVE_VISUAL_CORRECTION.md)

## 1. 方向纠正

这一阶段明确废弃了三种错误方案：

1. 不再把普通网页 Block 加旋转、margin 和纸张边框后称为“世界”。
2. 不再使用墓碑状立牌承载内容入口；场景中原有的浮动档案牌已经移除。
3. 不再把九个内容板块一一映射为九间相似木屋。主世界只保留少量具有功能和叙事差异的场所；音乐、电影和书籍进入独立室内世界。

当前产品结构改为：

- **主世界**：第一人称、可快速移动、可跳跃、具有物理碰撞、无限地形分块、海岸、河流、可攀爬山地、森林、雾、稳定碎片天气和真实生态。
- **中心住宅**：地图唯一住宅，位于海岸，正门可直接穿过而无转场页；窗、床、桌面、墙面、行李和收藏数据共同组成拥挤而温暖的生活空间。
- **档案 / 对话**：与世界并列为顶端三态切换；档案保留高密度二维阅读，对话提供同一视觉体系下的 AI 会话。

三态入口已经落地：页面不再出现 landing 或“进入世界”按钮，默认直接进入可控制的第一人称世界。顶端中央 `世界 / 档案 / 对话` switch 在三个状态间即时切换，Canvas 保持挂载以保留玩家坐标；档案继续渲染现有 `BlockCanvasEngine`，音乐和影视收藏卡片原样保留；对话复用现有 `/api/assistant` 流式接口，并以深森林绿、旧信纸和衬线字体重做为完整会话页面。

原 1290 行的 `ArchiveWorld.tsx` 已拆为 177 行状态壳，以及独立的模式切换、世界 HUD、真实河道小地图、背包、档案与对话组件；这一批新增世界模块均低于 300 行。背包内容同时按空间逻辑收敛为口粮、拾得纸片和测绘工具，收藏数据明确归属主屋与档案，不再假装成随身物资。

原 2423 行的 `ArchiveGameScene.tsx` 也完成第二次结构清理：主场景壳降至 137 行，地形、Shader、天气与收藏物、树木碰撞、真实资产结构、世界常量和类型分别进入 32–146 行的独立模块。旧代码中已不再渲染的九处场所、露天影院、车站、锯木场、程序化人物、程序化马匹、程序化远山与程序化植被实现被整体删除；运行场景只引用真实 Quaternius glTF / GLB 资产、物理碰撞、地形 Shader 和实例化自然层。

第一人称控制器同步从 363 行降至 300 行，马匹刚体、碰撞体和真实 Horse GLB 挂载被抽成独立 `ExplorerRigs`。地图传送的三份重复执行逻辑被收敛为事件处理与一份帧级兜底；浏览器从主屋快速传送到旧木桥后坐标准确落在 `X 43 / Z -160`、朝向 90°，桥面资产、碰撞和 HUD 状态均正常。

世界目录的 300 行边界已经全部满足：未再使用的 622 行 `ArchiveInteriorWorld` 被删除，防止九间空房方案重新进入运行图；`ArchiveWildlife` 降至 227 行，`QuaterniusForest` 降至 188 行。森林最外圈原先以圆柱、圆锥和十二面体程序化伪造树冠，现已全部删除，7×7 Chunk 统一使用真实 Quaternius 树木 glTF 几何实例；每 Chunk 密度调整为 11 株后，旧木桥实测约 54 draw calls / 3057382 triangles，控制台无错误。程序化三角形鸟群也已撤下，等待真实飞行动物资产接替。

## 2. 已落实的视觉语言

整体视觉遵循已确认的关键词：安静、克制、艺术、复古、怀旧、有人情味、自然、深邃。

- 主色：深森林绿、褪色灰绿、苔藓绿。
- 辅色：旧纸黄、暗赭石、褪色木色。
- 字体：中文衬线为主，方向、编号和坐标使用等宽体。
- 材质：旧纸颗粒、低饱和贴图、雾化纵深、边缘暗角。
- 动态：近千枚纸雪般碎片持续下落；室内也有更稀疏的漂浮碎屑。
- 编排：HUD 不再沿用普通网站顶栏，而是游戏状态顶栏、小地图、方向控制、任务提示、状态栏和背包。

全屏旧纸纹理层此前以 0.19 秒频率位移并改变对比度，造成雪片之外的整幅画面抖动，现已取消动画；地形颗粒哈希、雪片透明度与营火照明也改为稳定值。当前只有雪片的位置持续下落、河水表面自然运动和动物骨骼动画保持动态。

## 3. 主世界空间逻辑

### 3.1 地形

地形以玩家为中心动态维持 3×3 个 Rapier 高度网格分块。玩家跨越分块边界时，周围地形自动更新，因此没有一圈可见的硬边界。

高度由多层低频起伏、山脊质量场和河谷切割共同生成：

- 出生点只保留小范围相对平缓的林间空地；
- 十几米外开始自然抬升；
- 中远景形成可行走山坡与山脊；
- 只有河道影响区允许地表低于水位，避免水面把普通低洼地错误淹没。
- 唯一住宅在局部缓坡中自然落地；旧九处场址的人工平台已经撤销，远方地形重新保持连续山势。

旧的矩形道路网格已经删除。中央路径只通过地表 ShaderMaterial 的轻微颜色变化提示，并加入横向摆动，不再像铺在地上的长方形贴片。

远景使用跟随玩家平移的 2048×1024 山景球幕，只承担大气透视而不参与碰撞。纹理垂直采样偏移校准为 `-0.12` 并开启双向 `RepeatWrapping`：此前雪山主体落在世界地平线下方，只剩云层可见；校准后，桥头与山地测绘点的树冠间都能读到连续雪峰、近山和雾谷，同时没有可抵达边界。

### 3.2 植被与资产

主世界树林已从圆柱/圆锥组合切换为真实 glTF 资产，并由 `InstancedMesh` 动态铺设。树木、地形和 Rapier 障碍物使用同一套坐标种子，避免出现“视觉上有树但可以穿过”或“空气墙没有树”的不一致。林下的岩石、灌木、蕨类、短草和蘑菇也改用同一套真实资产，并以局部簇群而不是规则点阵动态生成；灌木只以较低比例出现在外围簇群，不再形成大面积黑色球状噪点。

当前外部资产来源：

- [Quaternius Stylized Nature MegaKit](https://quaternius.com/packs/stylizednaturemegakit.html)：常绿树、松树、扭曲树、岩石、灌木、蕨类、草和蘑菇；CC0。
- [Quaternius Medieval Village MegaKit](https://quaternius.com/packs/medievalvillagemegakit.html)：唯一书屋、旧站台木箱、测绘平台楼梯和唱片装置旧车所需的模块；CC0。只保留实际使用的 glTF 模块，纹理缩至 1024px，未把 153MB 原包整体放入项目。
- [Quaternius Fantasy Props MegaKit](https://quaternius.com/packs/fantasypropsmegakit.html)：露天制图台、卷轴、绳圈、旅行袋、长椅、木桶、手工具、旧灯和手推车；CC0。免费标准包原始压缩文件为 143MB，仓库只保留实际渲染的 14 件 glTF 道具，共享贴图缩至 512px，当前子集约 4.1MB。
- [Poly Haven Shelf 01](https://polyhaven.com/a/Shelf_01)：室内旧书架；CC0。
- [Poly Haven Gothic Cabinet 01](https://polyhaven.com/a/GothicCabinet_01)：室内旧柜；CC0。
- [Poly Haven Cheese Box 01](https://polyhaven.com/a/CheeseBox_01)：室内和营地旧木箱；CC0。
- [Poly Haven Chinese Tea Table](https://polyhaven.com/a/chinese_tea_table)：收藏空间中央阅读/试听桌；CC0。

仓库中的每组外部资产都带有 `SOURCE.md`，记录来源、许可和导入日期。

### 3.3 地标不再等于房子

九类资料仍有世界坐标，但场所形态按内容重新分配：

- 个人简介：完全露天的旅行营地，以动态营火、铺盖、手记、旅行袋、木箱和旧桶组成；已删除会从正面读成巨大墙面或房顶的斜布；
- 项目：三张彼此错位的露天制图台、两道不平行图纸绳和散落木箱，不设屋顶；
- 时间线：带断轨、枕木、旧长椅、行李和残存信号机的废弃站台，不再保留站房或雨棚；
- 教育：正常人体尺度的三脚测绘仪、图纸桌、旅行袋和木箱；删除五米高立柱、顶部圆环和石质台基，不再产生神龛或纪念物轮廓；
- 工作：带原木堆、锯齿圆锯和木料台架的河谷露天作业区；删除跨在圆锯上方的双立柱高横梁，不设屋顶或门框式结构；
- 音乐：三张尺寸、倾角和朝向均不同的黑胶随意倚在真实旧车与木箱旁；删除原有三根展示杆和长条台基，不设房屋或棚顶；
- 影视：露天放映场；
- 书籍：唯一保留的林间书屋入口，使用真实门、侧窗、木梁、藤蔓、瓦顶和烟囱 glTF 模块装配；房体压低并以侧向三分之四角度出现，门外分散阅读桌、长椅和木桶，不再正面居中展示三角山墙；
- 留言：两根高低不同木桩之间自然下垂的信件绳、偏置工作台与旅行杂物；删除原有双柱大横梁和中央高杆。

九个地标不再用同一个朝向面对出生点。每一类场所拥有独立角度偏移，使道路抵达时看到侧面、背面或斜向工作状态，而不是连续九次看到正立面对称构图。出生点原有四柱平顶棚、碰撞体和三堆重复木箱也已完全删除，入口画面只保留山坡、路径、森林和天气；真正承担“房子”语义的主世界建筑只有书屋一栋。

入口名称只在玩家进入交互范围后以 HUD 提示出现，不再在世界中竖九块牌子。

## 4. 第一人称控制与物理

- 第一人称镜头，不渲染移动中的玩家身体。
- WASD / 方向键移动；普通速度 12，Shift 疾跑速度 22。
- 空格跳跃；水中速度自动降低。
- 拖动鼠标环顾：向右拖动向右看，向下拖动向下看，已纠正早期反向问题。
- 玩家使用 Rapier Capsule 刚体，树木、地形和场所拥有静态碰撞体。
- 高度异常或跌出地形时自动回到当前地表。

## 5. 小地图与连续传送

左上小地图现在使用固定世界坐标，而不是把所有远处地标挤在圆周边缘。玩家箭头会在整张地图上移动。

每个地点同时具有 SVG 图形标记和对应的透明 HTML 命中按钮。这样既保留视觉图形，也避免 SVG `<g>` 在浏览器自动化和部分触摸环境中的不稳定点击行为。

传送包含三层保障：

1. React `travelRequest` 状态；
2. `archive-world:travel` 自定义事件；
3. 第一人称控制器在逐帧循环中核对尚未执行的 request id。

传送落点会根据地标相对世界中心的方向计算，玩家落在场所正面安全距离之外，镜头同时自动转向入口。开放设施的碰撞也拆分到柱子、屋顶、桌台等真实部位，不再用包住整个场所的大型透明方盒。传送后会清空旧的邻近地标缓存，确保连续传送到同一地点或从小世界返回后，`E 进入场域`提示仍能重新出现。

河谷 `X 59 / Z -160` 增加了第一处非数据库地标“旧木桥”。桥的位置由河道曲线和两岸真实高度计算后确定，连接西侧工作/时间线方向与东侧教育/音乐方向，而不是随意放在水面。桥面与双侧护栏使用 Medieval Village MegaKit 的真实木构 glTF，通过 `InstancedMesh` 重复为 28 块桥面与 28 段护栏，仅保留少量 draw call；Rapier 使用与桥面坡度一致的长条碰撞体。小地图新增菱形基础设施标记，可从西岸安全落点传送并正视桥面。

主世界生态不再只由树和固定道具构成。林缘加入 3 只鹿与 2 只狐狸，模型来自 Quaternius Ultimate Animated Animal Pack 的 CC0 glTF，保留完整骨骼与动作。动物会在 `Idle / Eating / Walk / Gallop` 间交叉淡入，围绕栖息区游荡，接近河水时回避，玩家逼近时沿反方向逃离；远距离传送后会重新选择玩家附近的安全林地，因此无限地形不会在走远后变成空世界。高空另有 28 只程序化迁徙鸟，以左右翼两个 `InstancedMesh` 完成整群绘制，飞行高度随玩家所在地形抬升。

个人营地的火焰已从单张大尺寸三角形 billboard 改为 3 层不同高度、偏移与随机种子的 Shader 火舌。每层边缘由时间噪声、摆动和断裂共同塑形，尺寸压缩到木柴范围内；上方另有 14 颗实例化火星随时间漂移、抬升和缩小，继续共享暖黄色辅助色，而不制造突兀的白色锥体。

17 段世界路径把出生点、九个数据库场所和旧木桥连接成东西两条主线与南北分支。路线不是额外铺设的矩形 Plane，而是由 `archiveWorldTrails.ts` 的共享坐标生成 GLSL 线段距离场，直接混入无限地形 Shader，随山坡和河岸连续起伏。同一份数据也驱动 Quaternius 树木、地被与 Rapier 树干碰撞的让路判定，避免出现“视觉上有路、物理上却撞树”的两套规则。

## 6. 九个数据库驱动的可行走小世界

个人、项目、时间线、教育、工作和留言已经删除原先的右侧 dossier。它们分别进入营火手记、项目工坊、旧车站候车室、山地测绘室、河谷工作间和边地邮路：

- 项目成为横向图纸，取下后读取真实项目描述、角色、技术栈、日期和链接；
- 主世界项目工坊拆除了原先容易再次形成“牌子/墓碑”联想的大块立板，改为三根不等高木桩、两条轻微倾斜的绳线和 7 张错位悬挂图纸；图纸包含横向批注与圆形印章，并用纸张、批注、印章、夹子四组 `InstancedMesh` 控制绘制开销；
- 旧车站删除了长椅后方的矩形信息板，并用 14 块实例化真实木地板替代整块水泥台面；保留信号灯、铁轨、长椅、行李车、箱包等铁路语义；
- 山地观测点删除了四根立柱与整块顶板组成的“庙亭”轮廓，改成三根向中心收束的斜木梁、竖向测量轮、横向测绘镜、低矮石质基座和侧置工作台，场所完全开放且没有屋顶；
- 露天影院删除了巨型厚背板和无法辨认的黑盒。幕布改由两根倾斜木桩、上方绳杆和四枚系绳环悬挂，Shader 持续播放带山影、月亮、颗粒、划痕、暗角与亮度抖动的旧胶片画面；放映机缩小并偏离中心，以双环卷盘、轮毂、辐条、镜头和铭牌形成可辨识轮廓；
- 边地邮路删除中央方盒，换成真实工作台与抽屉、两卷旧纸、环形印章、酒桶和旅行袋；上方五封来信仍保持高低错位悬挂。
- 经历成为带缺口轮廓的旧车票，房间地面延续铁轨和枕木；
- 教育成为观测记录，空间中央具有测绘镜；
- 工作成为工作单，空间保留木料和圆锯语汇；
- 留言成为吊挂信件，直接读取数据库中的昵称、时间和正文；
- 个人资料成为田野手记，保存简介、状态、价值观和关键词。

这些条目共用数据适配器、第一人称控制、物件取阅和细节检查能力，但通过不同物件比例、房间配色和空间装置表达内容，不再复用普通网页 Block。

### 音乐：雾中唱片室

- 数据来源：`data.library.music.works`。
- 专辑封面成为墙面可点击唱片封套，而不是右侧弹出的列表。
- 缺少封面的条目使用基于名称首字与创作者的程序化复古封套，不显示重复空白框。
- 点击封套后出现中央“取下查看”档案，保留原有封面、名称、创作者、分类、评论和链接。
- 场景包含真实旧柜、旧书架、木箱、茶桌和唱片机装置。

### 影视：林间暗房影院

- 数据来源：`data.library.film.works`。
- 海报成为放映墙入口；当前数据中的《情书》会居中展示，而不是因固定五列布局偏到左侧。
- 空间包含放映座位、投影机、暗绿墙面和真实旧家具。

### 书籍：页边痕迹图书室

- 数据来源：`data.library.book.works`。
- 三本书按当前行内数量自动居中，放置在旧书架空间中。
- 删除了容易产生“庙门”联想的双柱横梁，改为茶桌阅读灯和独立书架。

九个空间共享同一套可复用房间壳、第一人称控制器、真实家具组件、物件陈列和检查面板；差异由 realm 配置、数据适配器和场景装置驱动，避免重复代码和交互漂移。

## 7. 背包的实际逻辑

背包不再只是四张通往原卡片的按钮：

- 记录 9 个地标的发现状态；
- 发现记录写入 `localStorage`，刷新后仍存在；
- 显示真实数据库藏品总数；
- 初始携带 3 份田野口粮；
- 使用口粮会实际向第一人称控制器发送体力恢复事件并扣除数量；
- 收藏入口会区分“未踏勘”和“已抵达”，并回到对应空间陈列。

主世界另外散落 18 张具有暖色微光的旧纸片。玩家在第一人称移动中靠近即可拾取，纸片会从场景消失、任务栏更新并出现短暂的“FIELD PAGE RECOVERED”反馈。拾取进度同样写入 `localStorage`，背包以第四项补给状态展示 `已拾取 / 18`，因此探索和移动会产生可保存的结果，而不是只有看景和传送。

## 8. 技术职责

| 技术 | 当前职责 |
|---|---|
| React Three Fiber | 主世界与室内世界 Canvas、场景图和帧循环 |
| Three.js | 相机、灯光、雾、纹理、真实 glTF 场景对象 |
| Rapier | 玩家刚体、地形网格、树木和场所碰撞 |
| GSAP | 世界入口、室内进出雾化遮罩 |
| glTF / GLB | Quaternius 植被、木构桥、骨骼动画动物、书屋模块、旧车、楼梯与 Poly Haven 家具资产 |
| ShaderMaterial | 地形纹理混合、河水波动、动态营火、纸雪碎片 |
| InstancedMesh | 无限森林、河桥模块、迁徙鸟群和持续下落的碎片天气 |

主世界自然包的 11 张 2048px PNG（含树皮、法线、透明叶片、岩石、草和蘑菇）已在不改变尺寸和透明通道的前提下转换为 WebP，glTF 引用统一改写。纹理总量从约 24MB 降为约 1.5MB，自然资产目录从约 28MB 降为约 5MB；原始 PNG 已通过 Git-aware 删除，可从提交历史恢复。模型几何、材质槽、UV 与层级保持不变。

无限森林增加基于玩家所在 Chunk 的两级 LOD：近中景 5×5 Chunk 保留完整 Quaternius 树干、分枝和透明叶片；最外两圈仅以 3 个 InstancedMesh 绘制分层针叶冠、错位双阔叶冠与树干，并沿用道路、场址、河岸和出生点清场规则。轻量树冠使用褪色灰绿与秋褐色，不受远景阴影压黑。出生点冷启动实测从约 397 万三角面降到约 207 万，draw call 维持在约 115–122，近中景视觉资产不降级。

林下层由同一套 Quaternius CC0 资产扩充：每个地表 Chunk 从 20 个提高到 30 个实例，引入真实 `Bush_Common`，并重新分配岩石、灌木、蕨类、短草和蘑菇比例。实例仍按四个随机簇群生成，路径中心、桥头、交互核心和水下保持清空；场所边缘清场半径缩小，让自然层从外围侵入而不妨碍行走。最大实例容量提高到每类 240，但复用原 glTF 几何和 InstancedMesh，未按单株增加 draw call。

鹿与狐狸原先分别是 3.1MB、3.0MB 的 Base64 内嵌 glTF JSON。项目增加可复用的 `scripts/convert-embedded-gltf-to-glb.mjs`，把单一内嵌 Buffer 无损封装成标准 GLB；两个文件合计降至约 3.7MB，骨骼、蒙皮和全部动画 Clip 保持不变。动物不再在入口页预加载，仅在玩家点击“进入世界”后由局部 Suspense 异步挂载；程序鸟群仍在入口远空持续飞行。

骑乘系统改用 Quaternius Ultimate Animated Animal Pack 中的真实 Horse GLB，不再显示程序化几何马匹。玩家在出生林隙靠近后可按 `F` 上马，再次按 `F` 下马；步行、快跑、跃起分别驱动 Horse 原始骨骼动画中的 Idle、Walk、Gallop，并把移动速度、相机高度、体力消耗和 HUD 状态同步切换为骑乘参数。马匹等待位置、玩家挂载位置和快捷传送后的跟随位置复用同一组件与状态，不复制模型逻辑。

## 9. 内置浏览器实机验证

已在 `http://localhost:3000/` 验证：

- WebGL 正常运行，控制台无运行错误；
- 第一人称镜头中不再出现玩家模型；
- 出生点出现真实树林、近距离山坡和稀疏营地，不再是一排房屋；
- 小地图从 `X 0 / Z 22` 正确跳转至音乐、影视和书籍坐标；
- 传送到车站、观测点、唱片林地、书屋和邮路时均落在场所外并自动正视目标，不再落进墙体；
- 连续传送后入口提示会重新出现；
- 音乐空间完成“进入 → 点击专辑 → 查看详情 → 放回原位 → 返回主世界”；
- 影视空间和书籍空间均可独立进入并显示真实数据库条目；
- 项目工坊完成“进入 → 取下 OKF Anything 图纸 → 查看数据库详情与链接 → 放回原位”；
- 时间线以 4 张车票展示上海、北京、南京、青阳经历，邮路以 2 封数据库留言展示来信；
- 九个小世界逐项冷回归通过：个人 3 项、项目 5 项、时间线 4 项、教育 5 项、工作 2 项、音乐 30 项、影视 1 项、书籍 3 项、留言 2 项，合计全部条目均实际出现在对应可行走空间；
- 主世界重新检查项目、时间线和工作三个最容易产生“房屋/庙宇”联想的场址：均已拆除完整棚顶并保持可进入、可碰撞和可传送；
- 个人营地二次实机纠偏：最初的单坡遮布从正面仍像巨大建筑立面，现已完全移除，营地只保留露天生活痕迹，并以 ShaderMaterial 营火提供动态光源；
- 项目、站台、锯木场与营地中的方盒占位道具已替换为真实 Quaternius glTF 工作台、卷轴、长椅、手推车、绳索、斧、桶和旅行袋；冷加载及贴图路径无控制台错误；
- 小地图新增“旧木桥”基础设施传送点，浏览器实测落在西岸 `X 43 / Z -160`、自动朝东 90°，桥面、护栏、河水、两岸和 Rapier 碰撞同时可见；传送过程中发现并修复了旧地标 `E 进入`提示可能被同一帧竞态重新写回的问题；
- Quaternius 鹿群在项目工坊林缘实测可见，两次相隔 6.5 秒的浏览器截图中位置、队形与骨骼步态均发生变化；远空鸟群持续盘旋，新增生态运行后浏览器控制台无错误；
- 项目工坊冷重载复验：原来的大块浅色立板已完全消失，7 张旧纸在两层绳线上形成高低、大小、旋转均不一致的横向构图，批注与印章清晰可见；实测场景保持约 107 draw calls，控制台无错误；
- 共享路径网冷启动复验：从出生点依次传送到旧木桥 `X 43 / Z -160` 与个人营地 `X -10 / Z 7`，桥头路径在河谷地表连续显现并向东岸爬坡，路线中央无树木、地被或树干碰撞；传送与 WebGL 控制台均无错误；
- 个人营地营火二次复验：原先占据大块画面的半透明三角火焰已缩成木柴上方的 3 层独立火舌，14 颗火星连续上升，场景约 68 draw calls，控制台无错误；
- 地标轮廓专项回归：旧车站木板站台不再出现立板；山地观测点不再出现四柱顶板、水平“光环”和吊灯；露天影院可见动态旧胶片幕布与双盘放映机，放映机已移出中心视线；邮路中央工作台、卷轴和印章清晰可见。四处连续传送及控制台均无错误；
- 自然纹理压缩后在旧 PNG 完全删除的状态下冷启动通过：树皮、法线、透明叶片、岩石与地被均正常，网络路径无 404、控制台无错误；出生安全林隙由 12 米扩大到 20 米，入口主标题前不再被超大树干遮挡；
- 动物 GLB 延迟加载回归：入口页不挂载鹿狐模型但保留迁徙鸟；进入世界并传送到项目林缘后，鹿群的完整骨骼步态正常出现，旧 glTF 删除后无 404 或控制台错误；
- 森林 LOD 冷启动回归：入口与进入世界后的出生点近中景仍为完整贴图树木，最外圈分层树冠只在远坡与雾线内接替；状态面板记录约 115 / 122 draw calls、2066135 / 2068233 triangles，控制台无运行错误；
- 主世界“去庙化”冷启动与逐点回归：出生点原四柱平顶棚彻底消失；唯一书屋以带侧窗的矮屋侧面进入视野；教育点为正常比例测绘仪；项目为露天制图场；邮路为两桩下垂信绳。五处画面无神殿台基、牌坊式横梁或正立面对称建筑群，连续传送和控制台均无错误；
- 其余五处地标连续巡检：旅行营地、旧车站和露天影院本身保持开放构图；锯木场删除门框式高横梁；唱片林地删除三根展示杆与台基，三张黑胶改为不同角度倚靠旧车与木箱。九个主世界资料坐标全部完成“去庙化”视觉回归，室内音乐/影视收藏卡片未改动；
- 林下密度回归：个人营地外围与旧木桥两岸可见新增灌木、蕨类、草簇和岩石层，营地中心、桥面及共享路径仍保持净空；旧木桥落点约 74 draw calls / 2086013 triangles，新增真实植被后控制台无错误；
- 远山球幕实机校准：旧木桥视线尽头与山地测绘点树冠上方均能辨认连续雪峰、近山和雾层，不再只有一片云天；全景接缝不进入当前视野，连续传送和控制台无错误；
- 背包显示发现进度、口粮数量和 39 件当前数据库藏品；
- 从个人营地落点实际拾得第 1 / 18 张旧纸片，HUD 出现反馈，背包刷新为 `1 / 18`；
- TypeScript `npx tsc --noEmit` 与 `git diff --check` 通过。

## 10. 下一阶段质量项

当前基线已经从“网页加 3D 装饰”转成可玩的空间系统，但距离商业开放世界的资产密度、角色生态和叙事复杂度仍有差距。下一阶段优先级是：

1. 继续降低主世界 glTF 实例的无效三角形开销并增加 LOD；
2. 增加河岸、山体断面、动物声景和更多远景层次；
3. 在现有 18 张可收集纸片上增加分段任务、音效和完成奖励；
4. 继续降低规则排列感，让项目图纸、车票与来信在空间里产生更真实的错位、叠放和路径关系；
5. 继续用内置浏览器进行完整路线截图和交互回归。

## 11. 当前架构纠偏：不再以建筑数量充当内容密度

本节覆盖前文中已经被后续重构淘汰的“九个地标 / 九个室内世界”方案。用户明确指出大量重复房屋形成了庙宇群和模板化场址，因此主世界现只保留一座带开放门洞、正面与侧面窗户的临海主屋。其余旧地标、牌面、站台、工坊和对应的传送坐标已经从场景与小地图彻底删除。地图的空间密度将由海岸、河流、道路、森林、可攀爬山体、动物生态与主屋内部的真实生活内容建立，而不是继续复制建筑。

页面同时删除了进入世界的 Landing：访问后直接处于第一人称世界。顶部只有三个并行状态：

- **世界**：持续保留当前坐标、镜头、骑乘与收集状态；
- **档案**：复用现有 Block 数据引擎，音乐与影视的成熟卡片呈现完整保留；
- **对话**：使用现有 `/api/assistant` 流式接口，不再以 3D 空间替代所有阅读界面。

旧的 622 行室内世界和 2423 行主场景已经拆除。当前 `components/world/` 内所有 TypeScript / TSX 文件均不超过 300 行；地形、气候、结构、碰撞、森林、地被、动物、玩家、HUD、地图和三个页面状态分别由单一职责模块维护。

## 12. 真实动物生态与陪伴犬

本阶段新增资源均来自可追溯的 CC0 模型，不以几何基础体模拟动物：

- Quaternius Ultimate Animated Animal Pack：鹿、狐狸、马、柴犬、雄鹿、狼、羊驼与驴；
- Quaternius / Poly Pizza Bird：真实鸟类 GLB，用同一素材网格组成 18 只高空实例鸟群；
- 授权、来源与运行时处理记录在 `public/archive-world/quaternius-animals/SOURCE.md`。

12 只地面动物分布为鹿群、狐狸、雄鹿、狼、羊驼和驴。所有物种共享一套行为系统：在安全陆地初始化与重锚、定时选择漫游目标、Idle / Eating / Walk / Gallop 原始骨骼动画切换、靠近玩家后的物种化逃逸距离与速度。地面物种继续复用一个行为组件，通过配置文件控制差异，没有复制运动逻辑。

陪伴柴犬名为“苔苔”。它使用原始骨骼动画追随玩家，并依据摄像机方向驻留在玩家右后侧，避免挡住第一人称镜头；距离过远时会在无限世界中安全追赶。玩家靠近后 HUD 出现“蹲下与苔苔交谈”，点击直接进入陪伴犬 AI 对话。接口通过受限 `persona` 参数切换身份，仍只允许依据公开档案和当前上下文回答。

### 本阶段浏览器验收

- 出生点可见单一主屋、实际雄鹿和天空中的实例鸟群，不再出现建筑群；
- 地面动物与鸟群挂载后约为 107–113 draw calls、333 万 triangles，WebGL 状态正常；
- 柴犬第一版会冲到镜头中央，实机截图发现后已改为摄像机右后侧跟随，再次截图不遮挡中心视野；
- 靠近柴犬出现真实交互按钮，进入后标题、建议问题和消息署名均切换为“苔苔”；
- 在内置浏览器实际发送“你为什么跟着我？”，流式模型成功返回回复，证明不是静态假对话；
- 浏览器控制台日志为空，`pnpm exec tsc --noEmit` 与 `git diff --check` 通过。

## 13. 河流生物

Quaternius Animated Fish Pack 的三种小鱼已进入河流，海豚、蝠鲼、鲨鱼和鲸则进入海岸生态。最终运行时直接使用官方 FBX，通过 Three.js `FBXLoader` 保留原始骨骼空间、材质槽和 Swim 动画；完整来源与 CC0 授权记录在 `public/archive-world/quaternius-fish/SOURCE.md`。

河流当前有 18 条动画鱼，由共享组件按玩家所在河段重锚。它们沿 `riverCenterAt(z)` 的真实河道曲线逆流游动，而不是在直线或任意水面上漂浮；深度、横向泳道、速度、动画速率和体型具有确定性差异。河流数学函数从地形内部实现提升为共享导出，因此地形切口、鱼群路线和后续小地图都可使用同一个水系真值。

浏览器首次检查发现鱼群虽然运行，但 116 米路线过于稀疏，桥上无法肉眼确认；于是鱼群收束在玩家所在河段前后 26 米并靠近水面游动。中途尝试用 Assimp 转为 GLB，但浏览器定点检查明确发现蒙皮产生“扁平滑翔翼”和“竖直骨骼尖刺”，因此错误 GLB 已全部删除，不能以更方便加载为理由保留损坏素材。改为直接 FBX 后，河鱼和鲸豚恢复正确外形与动画，页面控制台无 404 或 WebGL 错误。快捷传送的最终落点仍保留在安全西岸 `X 43 / Z -160`，测试用水中落点没有进入正式配置。

## 14. 临海主屋与海洋生态

主屋西侧现在是由地形函数真实切出的连续海岸，而不是背景图或“临海”文字标签。`coastlineXAt(z)` 同时驱动地形沉降、岸线材质、海洋动物路径和小地图海陆分界；海岸向西持续进入约 8 米深的海床，河谷仍按独立的 `riverCenterAt(z)` 穿过旧木桥，因此海洋没有覆盖原有河谷。

- 海岸推进到主屋西侧约 18 米，门前出生与主屋传送点后移到 `Z 28`，建筑不再占满第一人称画面；
- 主屋仍是全地图唯一建筑，画面由海、水岸、森林、山坡、动物和一座屋共同组成；
- 小地图增加真实海面填充、岸线和“潮汐湾”自然地点；潮汐湾标记位于离岸水域，玩家落在安全观景岸，不在世界中增加牌子或房屋；
- 最初潮汐湾与主屋的 16px 点击区发生重叠，浏览器实际点击“主屋”会被潮汐湾截获；标记移到实际离岸坐标后，两个地点均可独立点击；
- 观景岸增加 25 米自然净空，只移除近景树木与地被，不建造亭台或观景建筑；
- 两只海豚在近岸浅跃，蝠鲼、鲨鱼和鲸在更深的离岸泳道活动；所有物种复用同一个 FBX 骨骼、材质和动画运行组件；
- 马匹传送后不再固定贴在玩家右前方，而是从四个候选位置选择高于水面的安全等候点，避免主屋宽景和桥头画面被马头遮挡。

内置浏览器冷重载后，出生点约为 120 draw calls、179 万 triangles、28 个场景节点，WebGL 正常。潮汐湾连续两张截图可见近岸海豚位置与姿态变化，海岸观景视线无树墙，开发日志为空。TypeScript 与差异检查通过。

## 15. 唯一主屋成为生活空间与数据库入口

针对“搞一堆房子像盖庙”的批评，本阶段继续遵守只保留一栋临海主屋的硬约束，没有增加第二栋建筑，也没有以外部牌子、展板或重复小屋承载内容。主屋内部改用 Quaternius Furniture Pack 的真实 CC0 家具：单人床、床头柜、书桌、椅子和书柜；既有的卷轴、旅行袋、木箱、长凳和壁灯作为生活痕迹错落嵌入。来源与授权记录在 `public/archive-world/quaternius-furniture/SOURCE.md`。

家具本身就是数据入口，不再在世界中摆放方正的数据面板：

- 点击书桌读取 `development.projects` 的真实项目手稿；
- 点击书柜读取 `library.book.works` 的藏书；
- 点击旧木箱读取 `library.music.works` 与 `library.film.works`，完整收藏卡片仍保留在“档案”模式；
- 点击床与床头柜读取 `basic`、`life` 中的近况、习惯与价值；
- 点击窗边长凳与信件读取 `messages`。

读取时世界保持在背景中，玩家移动暂停；档案采用褪色灰绿旧纸、双栏错位、批注、印章和非等距条目编排，而不是跳转到新的网页或覆盖成规则卡片墙。关闭后玩家仍停留在原来的室内坐标。室内新增暖色局部灯光和“点击家具读取记录”的环境提示，敞开门洞与实体墙碰撞保持不变。

### 本阶段浏览器验收

- 从主屋传送点 `X -11 / Z 28` 实际步行到左侧门洞，并无过渡页地进入室内；
- 室内可见真实床、床头柜、桌椅、书柜、长凳、木箱和灯具，灯光从原先阴暗展示调为暖色生活光；
- 在第一人称室内直接点击书桌，成功打开 `5 项正在被保存的工作`，OKF Anything、World Engineering、PALM、SAYLESS 和 iNon 均来自当前数据；
- 档案层以错位双栏显示，关闭后仍在原室内坐标；
- 切换到“档案”模式后，音乐收藏与影视收藏仍存在，原有 `BlockCanvasEngine` 路径未改动；
- 新增与拆分的世界代码文件均低于 300 行；TypeScript、差异检查和浏览器错误日志检查通过。

## 16. 以真实山地替代更多建筑

主屋东侧新增一条属于无限地形本身的连续山系，而不是不可接近的背景模型。主峰坐标为 `X 105 / Z -58`，后方山体继续向东延伸；地形网格精度由每块 22 段提高到 32 段，渲染网格与 Rapier `TrimeshCollider` 继续共享同一套高度函数，因此林谷、岩坡、雪线和峰顶都可以步行或骑乘抵达。

山体表面没有使用纯色几何冒充素材：岩坡使用 Poly Haven `Rocky Terrain 03`，雪线使用 `Snow 02`，均为可追溯 CC0 素材；海拔和坡度共同控制森林地表、岩石与积雪的渐变。林线以上只保留稀疏矮松，雪线以上去除大部分树木并增加真实 Quaternius 岩石实例，避免温带阔叶树长到峰顶。

从主屋林谷到山顶新增一条复用世界路径系统的可行走山径。第一版把山体直接推到林谷边缘，浏览器正视时整块坡面像墙一样占满屏幕；实机发现后改为把路径坡度直接刻入同一块地形，从约 0.8 米缓升到 41 米，并把路径两侧过渡宽度扩大到 26 米，形成有林谷、岩坡和雪盆的连续剖面。山顶放置原有 18 张可收集纸片中的一张，不增加牌子、亭子或新房屋。

小地图增加 `M / 雪线山脊` 的真实自然坐标、三层山地等高线和可点击入口；点击后落在林谷起步点，而非直接瞬移到峰顶，保留实际攀爬过程。

### 本阶段浏览器验收

- 点击“传送到雪线山脊”落在 `X 0 / Z 22`，镜头正对真实山谷入口；
- 初版山坡挡满视线的问题在同轮实机检查中被发现并修复，加宽后可同时看见土径、两侧林坡、岩石、树木、动物和上方雪盆；
- 实际按住疾跑沿山径从 `X 0 / Z 22 / ALT 0.6 M` 行进到 `X 29 / Z -1 / ALT 4.0 M`，继续到 `X 39 / Z -9 / ALT 5.8 M`，证明坐标、碰撞、坡度和海拔状态同步上升；
- 山脚与半坡可见真实鹿、岩石、针叶树和持续飘落的纸雪；超过 4.5 米后 HUD 正确切换为“山脊”；
- 浏览器错误日志为空，TypeScript 与差异检查通过。

## 17. 野外收集物改为真实卷轴与数据库札记

旧版 18 个野外收集物使用程序生成的发光平面，只能增加计数，不符合“素材必须真实”和“数据库内容应以绝妙方式呈现”的要求。本阶段全部替换为 Quaternius 道具包中的真实 `Scroll_2.gltf`，以共享 glTF 几何和材质通过 `InstancedMesh` 放置；拾取判定、地形高度与持久化逻辑保持复用，没有为 18 个卷轴复制组件。

每个卷轴现在对应一条从当前 `ReadmeData` 自动编排的真实田野札记，来源依次包括：格言、引用、个人哲学、个人愿景、生活要素、通知、地点经历、价值观和生活习惯。数据适配器稳定生成 `FIELD / 01` 至 `FIELD / 18`，不在场景中硬编码展示内容。

背包继续只保存野外物资，不塞入唱片、影片、书和主屋档案；新增“田野札记”区域，以褪色浅绿旧纸、错位旋转和深绿色底层展示已拾内容。拾取时 HUD 不再只说“得到一张纸”，而是直接浮现该条真实文字及其类别。

### 本阶段浏览器验收

- 冷启动等待真实 3D 资产加载完成后，WebGL 状态为 live，场景约 161 draw calls / 122 万 triangles；
- 从主屋出生点实际走到 `X 0 / Z 16`，拾取第一卷真实卷轴；
- HUD 即时显示数据库格言 `It's Now or Never` 与 `MOTTO · 1`；
- 打开背包后计数更新为 `1 / 18`，札记区实际显示 `FIELD / 01 · MOTTO`、原文和“写在页边的自我提醒”，证明不是只增加计数；
- 首次检查发现新札记 CSS 未被构建链加载，已合并回已验证的世界样式入口并通过计算样式确认浅绿色旧纸、内边距与错位旋转全部生效；
- 浏览器错误日志为空，TypeScript 与差异检查通过。

## 18. 渐进式场景载入

冷刷新实测曾出现近二十秒的深绿色空幕。原因不是世界缺少内容，而是山景、森林、动物、鱼群、住宅、地形纹理和 Rapier WASM 共用过大的异步边界；任何一个真实资产尚未就绪，都会暂停整张 Canvas。

当前按视觉叙事顺序拆为独立层：远山、森林与地被、陆生动物、水生动物、收集卷轴、住宅结构分别加载；物理引擎拥有外层 WASM 边界，内部地形、建筑和玩家马匹再各自拥有细粒度边界。马匹网格只延迟自身，不再阻塞玩家刚体与镜头。地形几何和碰撞也不再等待三张表面纹理，先以褪色森林灰绿材质出现，岩地、雪线与林地真实纹理就绪后无缝替换。

### 本阶段浏览器验收

- 修改前冷刷新 8 秒仍只有深绿色空幕，诊断帧停在 `WebGL initialising`；
- 进一步定位到 Rapier WASM 和玩家分支中的马匹 glTF 是两个独立根级悬挂点，并分别收束边界；
- 修改后冷刷新 3 秒已能看到唯一主屋、海岸、山坡、森林、马匹和纸雪，不再先出现空场；
- 玩家稳定停在 `X -11 / Z 28 / ALT 1.0 M`，没有因地形材质稍后覆盖而坠落；
- 完整场景恢复为约 161 draw calls / 122 万 triangles / 28 scene nodes，浏览器错误日志为空；
- TypeScript 与差异检查通过，相关世界文件仍低于 300 行。

## 19. 小地图改为无限世界局部测绘

地形、森林、地被、碰撞和动物原本已经按玩家坐标重锚，实际可以持续生成，但小地图仍把全世界固定投影在 ±285 米圆内。玩家越过该范围后箭头会被压在边缘，视觉上仍像存在隐藏边界。

小地图现在使用以玩家为中心的 300 米局部测绘窗：玩家箭头永远留在中心，海岸线通过 `coastlineXAt`、河道通过 `riverCenterAt` 按当前位置重新采样，保持与同一套地形数学一致。主屋、旧木桥、潮汐湾和雪线山脊在局部范围外时被压到地图边缘，仍保留可点击传送能力；进入范围后恢复真实相对位置与山地等高线。地图下方持续显示当前世界坐标，不再暗示固定地图边界。

### 本阶段浏览器验收

- 主屋出生点显示 `LOCAL 300 M · X -11 · Z 28`，玩家箭头居中，海岸与河流位置符合当前视野；
- 点击雪线山脊后地图重心移动到 `X 0 / Z 22`，玩家仍在中心，地形、雪盆和地图相对方位同步；
- 再点击旧木桥后地图重心移动到 `X 43 / Z -160`，河流穿过地图中心，远处地点自动收束到圆周；
- 两次地图点击均完成真实世界传送，玩家分别稳定在 `ALT 0.6 M` 与 `ALT 3.8 M`；
- 浏览器错误日志为空，TypeScript 与差异检查通过，组件为 138 行。

## 20. 可选择的真实空间声景

世界不再只有视觉层。新增两条可追溯的真实野外录音：nille 的公有领域森林环境声，以及 RandomMind 在 Vistula 河录制并以 CC0 发布的水浪环境声。原文件、来源页面、授权与 SHA-256 全部记录在 `public/archive-world/ambient-audio/SOURCE.md`，没有使用 Web Audio 振荡器或程序噪声冒充自然。

声景默认关闭且 `preload="none"`，不会给冷启动增加 14MB 音频请求。玩家主动点击顶栏“声景 OFF / M”后才开始加载与播放；混音直接复用真实世界函数：到 `coastlineXAt(z)` 的距离决定海岸水声，到 `riverCenterAt(z)` 的距离决定河谷水声，其余空间由林鸟、风和树林底噪占据。玩家移动时两个音轨缓慢交叉淡化，不跟随 React 帧硬切。

切换到“档案”或“对话”会暂停两条环境声，回到“世界”后在保留开启状态的前提下继续；键盘 `M` 与按钮使用同一个状态，不复制逻辑。

### 本阶段浏览器验收

- 冷刷新后按钮为 `声景 OFF M`，两条音频保持未预载；
- 在临海主屋点击后两条真实录音均达到 `readyState 4` 并实际播放，森林混音约 `0.168`、水声约 `0.260`，符合临海位置；
- 切到档案模式后两条音轨的 `paused` 均为 `true`；
- 回到世界后按钮仍为 `声景 ON M`，音轨恢复播放并继续向空间目标音量平滑变化；
- 浏览器错误日志为空，TypeScript 与差异检查通过；新声景组件为 76 行。

## 21. 第一人称植被遮挡系统

真实树木和地被虽然有深度遮挡与树干碰撞，但第一人称镜头贴近树冠、灌木或蕨类时仍可能被整块模型封住。新增的遮挡系统没有删除生态密度，也没有用纯代码假素材替换原模型：Quaternius 树木、岩石、灌木、蕨类、草和蘑菇继续使用原几何与材质，只在镜头邻域修改实例矩阵。

树木在距离玩家 1.1–3.8 米时通过 `smoothstep` 连续缩退，地被在 0.45–2.7 米时连续缩退；离开后按同一函数恢复，没有二值开关导致的闪烁或跳变。树木 Rapier 碰撞体独立保留，因此视觉净空不会让玩家穿过树干。森林与地被复用 `archiveWorldOcclusion.ts` 的唯一距离真值，实例缓冲改用 `DynamicDrawUsage`，并分别每 2 帧、每 3 帧更新以控制 CPU 与 GPU 上传成本。

### 本阶段浏览器验收

- 从旧木桥 `X 43 / Z -160` 使用屏幕方向键持续穿过整座桥并进入东侧森林；
- 连续移动到 `X 82 / Z -160` 和 `X 107 / Z -160` 时，近处仍有大量真实树干、树冠、灌木、蕨类和岩石，但镜头中心没有被贴脸叶片封死；
- 路线从河谷水域回到森林，海拔、地图重心和地形状态持续更新，没有因动态实例矩阵停止；
- 最终场景保持约 204–244 万 triangles，WebGL live，浏览器错误日志为空；
- TypeScript 与差异检查通过，森林 207 行、地被 175 行、共享遮挡函数 17 行。

## 22. 使用真实铰链直接推门进入主屋

唯一主屋原本只有敞开的门洞，虽能无转场走入，但缺少“看见门打开”的自然过程。当前门洞加入同套 Quaternius Medieval Village 素材中的真实 `Door_1_Round.gltf`；玩家靠近门廊时，门扇围绕素材自带的左侧铰链平滑向屋内开启，离开后关闭。门洞继续使用原有墙体碰撞，门扇不会制造第二个空间或切换页面。

第一版把门模型额外偏移 1 米，导致它绕门外空气公转并扫过镜头。浏览器发现后直接检查 glTF accessor：门几何 X 边界为 `-0.046 → 1.074`，确认模型原点本来就是左铰链；删除多余偏移后，门扇能够贴向室内左墙。出生点从 `X -11` 调整到门洞轴线 `X -13`，森林、地被和碰撞净空全部改为复用 `WORLD_PLAYER_SPAWN`，不再散落硬编码坐标。

### 本阶段浏览器验收

- 冷刷新出生于 `X -13 / Z 28`，不需要横移或转镜头；
- 连续按屏幕前进键到 `X -13 / Z 10`，真实圆拱木门已经向左贴墙，门槛、长凳、床与暖灯的视线完整开放；
- 继续直走到 `X -13 / Z 5`，无转场进入主屋并显示 `LIVED-IN ARCHIVE` 家具读取提示；
- 门扇没有挡住玩家，主屋仍是全世界唯一建筑；
- WebGL live，浏览器错误日志为空，TypeScript 与差异检查通过。

## 23. Minecraft 式自由飞行

世界新增可随时切换的第一人称飞行状态。`V` 或顶栏“飞行 OFF / ON”切换 Rapier 刚体的重力倍率；飞行时 WASD 保持水平移动，Shift 加速，Space 上升，Ctrl 或 C 下降，再按 V 恢复重力并自然落地。飞行不能与骑马同时开启，飞行状态下也不会错误触发涉水或骑乘提示。

移动端在原方向键右侧增加独立的上升、下降按钮，沿用 pointer-down / pointer-up 持续输入，不把长按退化为一次点击。HUD、顶部任务状态和右下状态栏同步显示飞行帮助、`空中悬停 / 自由飞行`、实时高度与速度。

为遵守 300 行限制，玩家控制器中的鼠标环顾抽成 `useExplorerPointerLook.ts`，正反方向保持此前已修正的 FPS 逻辑；加入完整飞行后 `FirstPersonExplorer.tsx` 仍为 285 行。

### 本阶段浏览器验收

- 顶栏点击 `飞行 OFF V` 后变为 `飞行 ON V`，HUD 出现完整飞行键位，移动端出现上升和下降；
- 在临海主屋出生点长按上升，从 `ALT 1.1 M` 升至 `ALT 6.2 M`，画面越过屋顶并看见海岸、森林、山坡和鸟群；
- 保持 `ALT 6.2 M` 水平飞行，从 `Z 28` 前进到 `Z 14`，证明不是只改相机高度；
- 长按下降回到 `ALT 0.0 M`，碰撞地形仍有效；
- 再升到 `ALT 6.3 M` 后关闭飞行，约 2.7 秒自然回落到 `ALT 0.0 M`，重力恢复；
- 浏览器错误日志为空，TypeScript 与差异检查通过。

## 24. 唯一住宅扩展为完整海边家园

世界继续严格保留一座中心住宅，没有通过复制房屋制造伪丰富度。主屋周围改为一个可读、可行走的生活院落：入口轴线保持直通房门，西侧临海篝火与木柴，东侧是褪色深绿泳池，屋后为花园和工作角，外围使用同套 Quaternius 木围栏形成院落边界。住宅、生活设施和自然地貌由一条连续视线串联，画面中仍能同时看见大海、雪山、丘陵和树林。

新增内容全部以真实模型为主体：泳池与花卉使用 CC0 glTF，篝火与木柴堆使用带署名许可的 glTF；原文件、作者、许可和来源页面记录在 `public/archive-world/poly-pizza-home/SOURCE.md`。现有 Quaternius 围栏、长凳、水桶、工作台、木桶和斧头继续复用，没有用 Box、Cylinder 等程序几何冒充可见资产。代码只负责布局、Rapier 碰撞、材质做旧和篝火点光。

第一轮浏览器侧视发现原骑乘马出生点 `[-2.5, 15]` 与泳池位置重合，马实际站在水中；将固定停驻点移动到院落入口东侧 `[3.5, 23]`，冷刷新后泳池恢复净空。木柴堆也按各自世界坐标重新采样地形高度，不再悬浮在海岸坡面。

### 本阶段浏览器验收

- 冷刷新正面视野只出现一座主屋，围栏、花园、泳池、篝火和木柴组成同一个家园，没有新增第二栋建筑；
- 左右环顾可分别看见临海篝火区、接地木柴堆，以及深绿色泳池、花园和雪山谷地；
- 马匹完整加载后不再进入泳池，家养驴仍可在院落活动，远处野生动物保持在山坡与林地；
- 主屋入口继续保持无遮挡，可直接推门进入室内；
- 完整加载为 `WebGL live · 210 draw calls · 1188421 triangles · 29 scene nodes`，浏览器错误日志为空；
- TypeScript 通过，新增院落组件 161 行，所有世界代码继续低于 300 行。

## 25. 补齐 Quaternius 动画动物包

同一 Ultimate Animated Animal Pack 中此前只抽取了当时会显示的 8 个模型，本阶段补齐三个不同种类：公牛、奶牛与哈士奇。它们不是现有动物的换色实例，而是各自独立的骨骼、网格和 12+ 原始动画；Base64 内嵌 glTF 继续通过项目脚本无损重打包为 GLB，单个文件从约 3MB 降至约 1.8MB。

三种动物复用唯一 `AnimatedAnimal` 游荡系统：会根据距离切换 Idle、Eating、Walk 与 Gallop，接近玩家时逃离，在无限世界远离玩家后按相对偏移重新锚定。公牛与奶牛位于东侧低地，哈士奇位于西北林道，不会进入主屋院落。

当前可明确区分的物种总数从 16 增至 19。浏览器热更新后 draw calls 从 210 增至 213，三套骨骼模型均进入场景；`WebGL live · 213 draw calls · 1190315 triangles · 29 scene nodes`，错误日志为空，TypeScript 通过。

## 26. 农场动物进入连续世界生态

新增羊驼、猪、巴哥犬、绵羊与斑马五个不同物种，全部来自 Quaternius Farm Animal Pack 的 CC0 原始模型。运行时直接使用 Poly Pizza 发布的同源优化 GLB，避免把 5 个 350KB 左右的 FBX 与单独贴图转成更大的运行负担；来源、包页与授权继续集中记录在动物 `SOURCE.md`。

不同版本的动画 Clip 采用 `Armature|Idle` 一类带骨架前缀的命名。动物加载器现在同时注册完整名称与最后一段语义名称，因此 Ultimate 包的 `Idle` / `Walk` 和 Farm 包的 `Armature|Idle` / `Armature|Walk` 共用同一套状态机，不需要复制游荡组件。具备 Walk/Run 的斑马正常切换步态，只有 Idle/Jump 的旧农场模型则保留 Idle 骨骼姿态并继续进行世界位移。

五种动物分散在西侧坡地、果园、林间小径、东部草甸和远侧草原，并在离玩家过远后跟随无限世界重锚。浏览器切换视线到山谷后，同一画面能看见新加入的黑色公牛、浅色牛/羊驼与原有鹿、鸟群共同活动；诊断达到 `WebGL live · 760 draw calls · 1144582 triangles`，错误日志为空。当前真实可区分物种从 19 增至 24。

## 27. 林地小动物与近地飞行生态

新增兔、青蛙、鸡和蜜蜂四个不同物种，均使用 Quaternius 以 CC0 发布的真实 GLB。兔与青蛙保留完整 Walk、Run、Jump、Idle 等骨骼动画；鸡没有 Walk Clip，通用状态机自动选择 Run 作为移动步态、`Idle_Peck` 作为觅食动作，不再以不存在的动作名回退；蜜蜂原模型没有动画，使用 14 个真实模型克隆沿玩家附近的不同半径、相位和离地高度自由飞行。

蜂群以 42 米距离重新锚定，既不会在无限世界中被甩掉，也不会锁死在中心主屋。模型从预览尺度进一步缩小到约 24–30 厘米的风格化尺寸，避免贴近镜头时喧宾夺主。兔位于苜蓿地，鸡在院落外小径，青蛙在河岸；它们与大型哺乳动物共享同一套地形与水位安全判断。

浏览器热更新曾出现整屏深绿遮挡；逐层关闭蜂群、地面动物后遮挡仍存在，冷刷新后同一套完整资产恢复正常，确定是热更新保留旧相机/场景状态而不是新模型体积。最终冷刷新为 `WebGL live · 217 draw calls · 1191819 triangles · 30 scene nodes`，正面主屋、海岸、泳池、动物均正常，错误日志为空。当前真实可区分物种从 24 增至 28。

## 28. 蝴蝶鱼与潮线螃蟹

第 29、30 个可辨识动物类型分别补入水下和海岸，而不是继续塞进陆地动物列表。7 条 Quaternius 蝴蝶鱼使用原始 `Swimming_Normal` 骨骼动画，在玩家附近的海岸线外侧分层群游；5 只 Quaternius 螃蟹使用 `Walk` 动画，持续根据 `coastlineXAt` 和 `terrainHeightAt` 沿真实潮线上岸横移。两者均为 CC0 原始 GLB，来源和动画名记录在独立 `quaternius-coast/SOURCE.md`。

FBX 鱼类与 GLB 海岸动物原本会重复材质克隆、骨骼混合器和帧更新逻辑。新增 `AnimatedAnimalScene` 作为唯一动画场景核心，现有 `AnimatedAquaticAnimal` 只负责 FBX 加载，海岸组件只负责 GLB 加载和路径计算；修改一处即可同步阴影、粗糙度、色调与 Clip 选择。

浏览器冷刷新后为 `WebGL live · 221 draw calls · 1194534 triangles · 31 scene nodes`，近海水下能看到鱼群轮廓，潮线与主屋前方能看到螃蟹移动，错误日志为空。此时“可辨识动物类型”达到 30；但严格按生物学物种合并公牛/奶牛、鹿/雄鹿与多个犬种后仍不足 30，因此不把当前阶段作为物种目标完成，后续继续加入独立物种。

## 29. 严格合并重复模型后达到 30 种生态类型

新增蛇、黄蜂、鼠、蜘蛛与熊猫五种独立动物，全部来自 Quaternius CC0 动画动物资产。蛇、鼠、蜘蛛与熊猫复用地面动物的逃逸、游荡与无限世界重锚系统；黄蜂使用原始 `Wasp_Flying` 骨骼动画，在玩家附近维持一个不断迁移的小型空中群落。带有 `Snake_`、`Rat_` 等物种前缀的 Clip 现在会被统一归一化为 `Idle`、`Walk` 等语义动作，不再为每个模型复制动画状态机。

物种目录明确合并公牛/奶牛、鹿/雄鹿以及狼和不同家犬，不把性别、品种和颜色变体重复凑数；合并后仍有 30 个独立生态类型。右下 HUD 直接显示 `生态 30 种`，数字来自同一份可审计目录，后续增删物种只需修改一处。

此前所有地面动物共用同一个 Suspense 边界，任意一个大模型未加载都会阻止整个动物层出现。现在每只动物使用独立边界，先下载完成的物种先进入世界。浏览器禁用缓存后的冷启动约 11.7 秒出现完整世界与动物，明显短于此前约 23 秒的整批阻塞；稳定状态为 `WebGL live · 233 draw calls · 1205408 triangles · 31 scene nodes`，错误日志为空。

## 30. 小地图标记与真实落点统一

旧配置中“雪线山脊”的地图标记位于 `[105, -58]`，实际却把玩家送回主屋附近 `[0, 22]`；“潮汐湾”标记位于深海 `[-92, -12]`，实际落点又是主屋旁 `[-18, -8]`。这会让随玩家移动的地图看似正常，但标记、地貌与传送结果互相矛盾。

当前雪线山脊的标记和落点统一为真实山顶 `[105, -58]`，镜头朝向山下家园；潮汐湾统一为实际海岸线旁 `[-27, -98]`，并在植被生成器中共享同一处净空，不再复用主屋附近的旧坐标。主屋与木桥也逐一重新验证。

### 本阶段浏览器验收

- 点击雪线山脊后，HUD 从任意位置更新为 `X 105 · Z -58`，实际画面位于 `ALT 41.0 M` 的积雪双峰之间，可俯视山下森林、泳池与海岸；
- 点击潮汐湾后更新为 `X -27 · Z -98`，实际画面到达浅滩，可近距离看到蝴蝶鱼、海豚和潮线；
- 点击旧木桥后更新为 `X 43 · Z -160`；
- 点击临海主屋后更新为 `X -13 · Z 28`，回到唯一主宅入口；
- 四个按钮均只有一个可访问定位器，传送后 WebGL 保持 live，浏览器错误日志为空；
- TypeScript 与差异检查通过。

## 31. 用宿营地与木筏代替第二批房屋

世界不再通过复制房子增加内容。雪线山脊加入一处有明确功能的登山宿营点：Quaternius Survival Pack 的真实帐篷、背包、篝火和木料沿山顶步道集中摆放；潮汐湾加入同包木筏与船桨，停在传送落点正前方的近岸水面。六件资产均为 CC0 原始 GLB，来源、作者和模型页记录在 `quaternius-survival/SOURCE.md`。

山顶原本是两片陡坡之间的一条窄缝，放置任何道具都会被前景雪坡吞没。地形函数现在只在山顶 5 米核心范围形成可站立平台，并在 14 米内平滑回到原山体；山路、积雪材质与 41 米海拔均保留。玩家到达时既能看见宿营生活痕迹，也仍能越过帐篷俯瞰森林、主屋、海岸和远方雪峰。

生态分区同步收紧：蜜蜂和黄蜂只在低海拔活动，体型缩小到原先约三分之一；山顶只保留鹿、狐、雄鹿与狼等林脊动物，不再出现斑马、牛、羊驼和家犬扎堆雪线的无逻辑画面。回到低地后完整动物层会重新出现并自动重锚。

### 本阶段浏览器验收

- 雪线山脊落点保持 `X 105 · Z -58 / ALT 41.0 M`，帐篷、背包、篝火与木料构成单一可读宿营点；
- 宿营点没有新增房屋，帐篷尺度经过三轮截图调整，不遮挡山谷视线；
- 潮汐湾 `X -27 · Z -98` 正前方可见真实木筏，鱼群、海豚和水面仍正常渲染；
- 山顶不再出现巨型蜜蜂与低地家畜，回到主屋后动物层恢复；
- 主屋正面仍只有唯一一栋住宅，院落、泳池、围栏和海岸视线未丢失；
- 浏览器错误日志为空，所有可见新增物均来自下载资产而非程序几何。
