# iNon

过去的 native 和 docs 未必依然生效，以当前 CLAUDE.md 为最新标准。

## 产品

### 权限设计

#### 用户设计

- 未登录用户：可读所有用户的公开页
- 已登录用户：可读写自己的控制台、读所有用户的公开页
- 管理员：可读写后台管理页、读所有用户的公开页

#### 路由设计

- 每个用户的公开页：/:slug
- 每个用户自己的控制台：/i/:slug
- 后台管理页：/admin

对于单个用户，用户名只有一个，slug 可以有多个，slug 可以为空，即 / 和 /i/。用户名全局唯一，slug 全局唯一。

### 功能设计

#### 控制台设计 /i/:slug

- 顶栏
- 悬浮侧栏

侧导菜单项为：

- 控制台
可以理解为给自己用的公开页，不过仅自己可见。
  - 常用网站入口
  - 常用项目入口

- 内容管理
所有 Non 组件中的字段值的 CRUD。

- 公开网站
不是 iframe 预览，而是：决定公开页中，所有 Block 的是否展示和顺序、侧边栏的菜单项及每个菜单项和 Block 的对应关系。
拥有一个拖拽的能力，能把 Block 从 Block 列表中拖到主区域并且能自由调整位置、并排或上下（响应式大小），就像画板。
使用户在这里配置的效果，和公开页面的效果一致，只不过公开页面只读。

### 公开页 /:slug

- 顶栏
- 悬浮侧栏

即每个用户的个人网站。有很多 Non 组件，由用户自己配置。

### 后台管理页 /admin

仅负责资产库（对象存储）

## 技术

- 数据库：Supabase
其他技术请见 package.json，务必物尽其用。

### 原子定义

Non

用同一套组件系统同时驱动「首页」和「我的网站」，本质上是把 iNon 变成一个 **block-based 的操作系统**。组件的粒度决定了这个系统的上限。

例如：
- **网站收藏夹** — 常用网站网格
- **项目卡片** — 单个项目的封面、状态、摘要、快速跳转
- **音乐卡片** — 音乐人、专辑收藏网格
- **影片海报墙** — 影片网格
- **书架** — 书目网格
- **游戏收藏** — 游戏王哥网格
- **个人简介卡** — 头像、姓名、bio、社交链接等
- **AI 分身入口** — AI 对话
- **动态时间线** — 公开的个人里程碑
- **友链** — 友情链接网格