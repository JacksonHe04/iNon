{
  "name": "@inon-sso/worker",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "build": "wrangler deploy --dry-run",
    "test": "vitest run",
    "typecheck": "tsc --noEmit && tsc --noEmit -p test/tsconfig.json",
    "types": "wrangler types worker-runtime.d.ts --include-env false"
  },
  "dependencies": {
    "@better-auth/oauth-provider": "1.6.25",
    "@inon/sso-contracts": "workspace:*",
    "better-auth": "1.6.25",
    "hono": "4.12.32",
    "resend": "6.18.0",
    "zod": "3.23.8"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "0.18.8",
    "typescript": "6.0.3",
    "vitest": "4.1.10",
    "wrangler": "4.114.0"
  }
}
